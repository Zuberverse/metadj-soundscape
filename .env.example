# Scope API endpoints
NEXT_PUBLIC_SCOPE_API_URL=http://localhost:8000
SCOPE_API_URL=http://localhost:8000

# Proxy controls
# The proxy is disabled by default in production for security.
# Enable with SCOPE_PROXY_ENABLE=true only when your deployment has proper
# access controls (Vercel Password Protection, VPN, etc.).
# Production writes are limited to same-origin browser requests and
# endpoint/method allowlisted Scope operations.
SCOPE_PROXY_ENABLE=false
